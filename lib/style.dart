import 'package:flutter/material.dart'; //Import this so you can access the runApp function & core widgetss

//Main BBody
const appBGColor = Color.fromARGB(255, 71, 10, 121);

//Color Pallatte 
const primaryColor = Colors.white;
const secondaryColor = Colors.deepPurple;
const tertiaryColor = Color.fromARGB(255, 255, 230, 0);

//Colors With Opacity
const sixtyPercentWhite = Color.fromARGB(150, 255, 255, 255);

//Font Styling
const titleFontStyle = TextStyle(fontSize: 28, color: tertiaryColor);
const paragraphFontStyle = TextStyle(fontSize: 18, color: primaryColor);
const bbuttonFontStyle = TextStyle(fontSize: 20, color: primaryColor);

//Default Padding
const imagePadding = EdgeInsets.only(bottom: 40.0);
const buttonPadding = EdgeInsets.all(20.0);
const titlePadding = EdgeInsets.only(top:20.0, bottom: 20.0);
const paragraphPadding = EdgeInsets.only(top:10.0, bottom: 10.0);

//default Image Styling
const largeImageWidth = 300.0;
const mediumImageWidth = 150.0;
const smallImageWidth = 75.0;